import 'package:flutter/material.dart';

class JokeColumn extends StatelessWidget {
const JokeColumn({ super.key });

  @override
  Widget build(BuildContext context){
    return const Column(
                  children: [Text("joke")],
                );
  }
}